# CitiusTech brand assets

Supplied by the workspace owner:

- `citiustech-wordmark.png` is an unchanged copy of `CT Logo with text.png` in the repository root.
- `citiustech-mark.jpeg` is an unchanged copy of `CT Logo.jpeg` in the repository root.

The full wordmark appears in the login masthead. The CT mark appears in the expanded and collapsed application navigation, loading state, and browser icon. CSS frames remove the source images' surrounding white margins without stretching or recoloring the artwork. The original files remain untouched.
