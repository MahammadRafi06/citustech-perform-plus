# Landing analytics screenshots

Open `index.html` for the gallery. Captures use a 1440 × 900 desktop viewport; individual reports are cropped from rendered sections. The previous overview is retained in `before/`.

The provider panel shows additional HCCs confirmed, suspect rules closed and closure rate. The model comparison, social attributes and provider history are sample analytics. See `docs/LANDING_ANALYTICS_TODO.md` for verification and data boundaries.
