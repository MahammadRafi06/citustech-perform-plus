# Agent configuration UI

Captured in Chrome on a 1920 px desktop workspace after the UI rendered, September 13, 2026. Most captures used a 1920 × 993 viewport; the final overview reflects the shorter available browser window. Screenshot output dimensions also vary slightly when the browser includes a scrollbar. Inspected during the browser walkthrough. Settings shown are local configuration metadata; provider connectivity and inference are not enabled.

- `01-agent-registry.jpg` — five configurable roles; planned roles are available through the filter.
- `02-agent-settings.jpg` — model selection, response controls and context allocation.
- `03-instructions-and-tools.jpg` — locked core guidance, supplemental instructions and role tools.
- `04-version-history.jpg` — saved revisions and restore-as-draft.
- `05-provider-connections.jpg` — reusable provider details with actual unconnected states.
- `06-provider-details.jpg` — provider-specific metadata form.
- `07-model-catalog.jpg` — reusable model entries.
- `08-model-details.jpg` — Azure deployment configuration.

- `09-private-provider.jpg` — Private provider selection with an organization-hosted endpoint.

The registry and private-provider captures include the subsequent separate Agents sidebar entry; earlier detail captures document the form layout before that navigation refinement.

- `10-private-model-selection.jpg` — Private provider available directly when adding a model.
